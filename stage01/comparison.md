# Human vs AI Comparison

Question Human version AI version
Easy to understand? Yes. The code uses basic variables, lists, dictionaries and functions that I can understand. The AI version was also relatively easy to understand because it used basic Python features.

Runs successfully? Yes. I ran the program and it displayed the appointments correctly. Yes. I ran the AI-generated code and checked the output.

Uses only required features? Yes. It uses variables, lists, dictionaries, functions and basic validation. Yes. The AI version did not use a database or GUI.

Adds assumptions? The human version assumes that the appointment information is already known and entered into the code. The AI version made assumptions about how appointments should be stored and displayed.

Handles errors? The enhanced version checks whether the patient name is empty and raises a ValueError. The AI version provided some basic validation, but I had to test it to confirm its behaviour.

Could I explain it? Yes. I can explain the variables, list, dictionary, functions and validation. I can explain the main parts after reviewing and testing the AI-generated code.
